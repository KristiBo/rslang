enum ANSWER {
  WRONG = 'WRONG',
  RIGHT = 'RIGHT',
}

enum PAGE {
  HOME = 'home',
  TEXTBOOK = 'textbook',
  GAMES = 'games',
  STATISTIC = 'statistic',
}

enum METHOD {
  GET = 'GET',
  POST = 'POST',
  PATCH = 'PATCH',
  PUT = 'PUT',
  DELETE = 'DELETE',
}

enum ICON {
  SPEAKER = 'speaker',
}

export {
  ANSWER, PAGE, METHOD, ICON,
};
